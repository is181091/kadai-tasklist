<header class="mb-4">
    <nav class="navbar navbar-expand-sm navbar-dark bg-dark">
        <% if logged_in? %>
            <a class="navbar-brand" href="/">TaskList</a>
            <!-- ハンバーガーボタン-->
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent">
                <span class="navbar-toggler-icon"><span>
            </button>
        
            <!-- メニュー項目-->
            <div class="collapse navbar-collapse justify-content-end" id="navbarSupportContent">
                <ul class="navbar-nav">
                    <li class="nav-item"><%= link_to '新規タスクの登録', new_task_path, class: 'nav-link' %></li>
                    <li class="nav-item"><%= link_to 'ログアウト', logout_path, method: :delete, class: 'nav-link' %></li>
                </ul>
            </div>
        <% else %>
            <div class="collapse navbar-collapse justify-content-end" id="navbarSupportContent">
                <ul class="navbar-nav">
                    <li class="nav-item"><%= link_to 'ユーザ登録', new_user_path, class: 'nav-link' %></li>
                    <li class="nav-item"><%= link_to 'ログイン', login_path, class: 'nav-link' %></li>
                </ul>
        <% end %>
    </nav>
</header>